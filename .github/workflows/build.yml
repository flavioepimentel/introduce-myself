name: deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - name: Action checkout in main
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
        with:
          node-version: '16'
      - name: Build web-app
        run: |
          npm ci
          npm run build
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build